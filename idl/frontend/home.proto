syntax = "proto3";

package frontend.home;

option go_package = "frontend/home";

import "api.proto";

message HelloReq {
   string Name = 1[(api.query)="name"];
}

message HelloResp {
   string RespBody = 1;
}

message Emtpy {}

service HomeService {
   rpc Home(Emtpy) returns(Emtpy) {
      option (api.get) = "/";
   }
}